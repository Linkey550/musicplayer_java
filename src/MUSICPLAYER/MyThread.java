package MUSICPLAYER;

import java.io.FileInputStream;

import static MUSICPLAYER.JLayer.play;
import static MUSICPLAYER.MUSICPLAYER.musicflag;

public class MyThread extends Thread {

    @Override
    public  void run() {

        //player.play(mp3file.get_file(file,(int)itemEvent.getItem()));
            }
}
